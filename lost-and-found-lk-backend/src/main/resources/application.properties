spring.application.name=lost-and-found-lk-backend

# MongoDB Configuration - Use environment variable if available, otherwise fallback to default
spring.data.mongodb.uri=${SPRING_DATA_MONGODB_URI:mongodb+srv://todoListApp:sinhalanews@cluster0.gdcsnzk.mongodb.net/lost_and_found?retryWrites=true&w=majority&tls=true}

# JWT Configuration - Use environment variables if available
jwt.secret=${JWT_SECRET:5367566B59703373367639792F423F4528482B4D6251655468576D5A71347437}
jwt.access.ttl=${JWT_ACCESS_TTL:15m}
jwt.refresh.ttl=${JWT_REFRESH_TTL:7d}

# CORS Configuration - Use environment variable if available
app.cors.allowedOrigins=${APP_CORS_ALLOWEDORIGINS:http://localhost:5173,https://lost-found-site.vercel.app}

# Server Configuration
server.port=${SERVER_PORT:8082}
